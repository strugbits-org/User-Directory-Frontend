import {
  makeStyles,
} from '@material-ui/core/styles';

const useStyles = makeStyles({
  imagePreview: {
    width: '150px',
    height: '130px',
    border: '1px solid gray',
    marginTop: '10px',
  },
});

export default useStyles;